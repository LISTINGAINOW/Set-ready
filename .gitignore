# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# vercel
.vercel

# MED-6: Data files that contain PII — keep locally but do not commit.
# NOTE: seed JSON files in data/ are tracked (needed for build).
# Runtime-generated files are gitignored:
data/leads_*.json
data/email-campaigns.json
data/email-templates.json
data/free-listing-signups.json
data/media-outreach-list.json
data/outreach-log.json
data/partnership-emails.json
data/property-analysis.json
data/property-import-errors.json
data/property-import-log.json

# typescript
*.tsbuildinfo
next-env.d.ts
